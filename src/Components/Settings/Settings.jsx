import React from 'react';
import SettingsStyle from './Settings.module.css';

const Settings = (props) => {
    return (<div className={SettingsStyle.Settings}>

        Settings

    </div>);
};

export default Settings;